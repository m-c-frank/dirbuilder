#!/bin/bash

# Exit immediately if a command exits with a non-zero status.
set -e

# Check dirbuilder's help message
dirbuilder -h

# Print a success message
echo "Package seems to work as expected!"
